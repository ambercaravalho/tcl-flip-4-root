# Runtime dependencies for the vendored edl tool (vendor/edlclient).
# edlclient itself is NOT listed here - it is vendored (patched) in this repo.
# Install with: scripts/setup.sh   (or: pip install -r requirements.txt)
#
# Versions below are the ones this repo was verified against on Python 3.14
# (macOS, Apple Silicon). Loosen the pins if you need a different platform.

# --- USB / serial transport (the actual device I/O) ---
pyusb==1.3.1
pyserial==3.5
usb==0.0.83.dev0

# --- edl core deps ---
colorama==0.4.6
docopt==0.6.2
lxml==6.1.1
pylzma==0.6.1
pycryptodome==3.23.0
pycryptodomex==3.23.0
requests==2.34.2
passlib==1.7.4
Exscript==2.6.32

# --- optional: only needed for asm/patch tooling; silences the
#     "Capstone/Keystone library is missing" startup warnings ---
capstone==5.0.9
keystone-engine==0.9.2
